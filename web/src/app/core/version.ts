// App version + a short label shown in the footer. Kept as a plain constant so
// it works in a static GitHub Pages build with no server. Bump alongside
// releases (or wire to package.json via a build step later).
export const KATL_VERSION = '0.1.0';
